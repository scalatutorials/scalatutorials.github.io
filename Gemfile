source "https://rubygems.org"

# Mirrors the exact Jekyll environment GitHub Pages uses to build master.
gem "github-pages", group: :jekyll_plugins

# Required by `jekyll serve` on Ruby 3+.
gem "webrick"
